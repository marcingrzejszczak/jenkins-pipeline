apply plugin: 'groovy'

sourceSets {
    main {
        groovy {
            srcDirs 'jobs', 'src/main/groovy'
        }
        resources {
            srcDirs 'src/main/resources', new File(project(":common").projectDir, 'src/main/bash')
        }
    }
}

repositories {
    jcenter()
    mavenCentral()
    maven { url 'http://repo.jenkins-ci.org/releases/' }
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.7'
    compile 'org.jenkins-ci.plugins:job-dsl-core:1.50'
    testCompile('org.spockframework:spock-core:1.0-groovy-2.4') {
        exclude module: 'groovy-all'
    }
    testCompile 'xmlunit:xmlunit:1.6'
}

test {
    testLogging {
        exceptionFormat = 'full'
    }
}

task workspace(type: Copy) {
    from 'src/main/groovy'
    from 'src/main/resources'
    from new File(project(":common").projectDir, 'src/main/bash')
    into 'build/workspace'
}